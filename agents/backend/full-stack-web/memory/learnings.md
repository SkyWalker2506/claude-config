# Learnings

> Web'den, deneyimden veya diger agentlardan ogrenilenler.
> Format: tarih + kaynak + ogrenilen + nasil uygulanir

<!-- Entries will be added by the agent when discovering useful information -->

## 2026-08-13

- In repositories with both local and hosted UIs, trace launch buttons and
  documented URLs before editing a similarly named HTML form. A correct local
  UI change can remain invisible when the product path points at a static host.
