#!/bin/bash
MODEL="${CLAUDE_LOCAL_MODEL:-ollama/qwen2.5-coder:7b}"
if [ "${1}" = "bypass" ]; then
  shift; exec claude --model "$MODEL" --dangerously-skip-permissions "$@"
else
  exec claude --model "$MODEL" "$@"
fi
